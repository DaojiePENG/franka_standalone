# ──────────────────────────────────────────────────────────────────────────────
# LeapBot 控制器配置
#
# 修改此文件后直接启动即可，无需加命令行参数。
# CLI 参数会覆盖此文件中的值（优先级：CLI > 此文件 > 硬编码默认值）。
#
# 使用方式：
#   python leapbot_control.py                           # 使用此文件
#   python leapbot_control.py --config /path/to/other.py  # 指定其他配置
#   python leapbot_control.py --server_ip 1.2.3.4       # CLI 覆盖
#
# 标定工具：运行 calibrate_workspace.py 后将输出的边界值填入下方 SAFETY 部分。
# ──────────────────────────────────────────────────────────────────────────────

# ═════════════════════════════════════════════════════════════════════════════
#  网络
# ═════════════════════════════════════════════════════════════════════════════

SERVER_IP   = "192.168.1.100"    # GPU 推理服务器 IP（必填，按实际修改）
SERVER_PORT = 8000               # GPU 推理服务器端口
ROBOT_IP    = "192.168.3.2"        # franka_server.py 所在主机（通常为本机）
ROBOT_PORT  = 4242               # franka_server.py ZeroRPC 端口
TIMEOUT     = 5.0                # 推理请求超时（秒）

# ═════════════════════════════════════════════════════════════════════════════
#  任务与控制
# ═════════════════════════════════════════════════════════════════════════════

TASK            = "move_objects_into_box"   # 任务 ID（须与 GPU 服务器一致）
FREQUENCY       = 10                        # 控制频率 (Hz)
ACTION_HORIZON  = 4                         # 每次推理执行的动作步数
                                            #   1 = 纯 receding horizon（最灵活）
                                            #   4 = 推理频率 2.5Hz（推荐）
                                            #   运行时可按 +/- 键调整

# ═════════════════════════════════════════════════════════════════════════════
#  相机 → 模型视图映射
#
#  可用相机名：zed_0, zed_1, fisheye, l515_0
#  不同实验可能需要不同视角，修改下方三个变量即可。
# ═════════════════════════════════════════════════════════════════════════════

CAM_GLOBAL = "zed_0"            # global_image（主第三视角）
CAM_WRIST  = "fisheye"          # wrist_image（夹爪第一视角）
CAM_VIS    = "zed_1,l515_0"     # 可视化窗口中的额外相机（逗号分隔）

# 以下为 True 时禁用对应相机（不想接某个相机时打开）
NO_L515    = False
NO_FISHEYE = False
NO_ZED     = False

# ═════════════════════════════════════════════════════════════════════════════
#  安全限制
# ═════════════════════════════════════════════════════════════════════════════

# 单步变化上限（防止策略输出异常大动作）
MAX_DELTA_POS = 0.08            # 单步最大位移 (m)
MAX_DELTA_ROT = 0.3             # 单步最大旋转 (rad)

# ═════════════════════════════════════════════════════════════════════════════
#  工作空间边界（Franka base frame，单位：m）
#
#  首次使用前必须运行 calibrate_workspace.py 标定：
#    python calibrate_workspace.py --robot_ip localhost --margin 0.02
#
#  将标定输出的 safe bounds 填入下方。
# ═════════════════════════════════════════════════════════════════════════════

SAFETY_X_MIN = 0.20             # 前方最近
SAFETY_X_MAX = 0.70             # 前方最远
SAFETY_Y_MIN = -0.40            # 右侧最远
SAFETY_Y_MAX = 0.40             # 左侧最远
SAFETY_Z_MIN = 0.05             # 最低
SAFETY_Z_MAX = 0.60             # 最高

# ═════════════════════════════════════════════════════════════════════════════
#  启动行为
# ═════════════════════════════════════════════════════════════════════════════

SKIP_HOME = False               # True = 启动时跳过归零动作
MOCK      = False               # True = Mock 模式：只采集相机 + 推理
                                #         不连接机器人，打印预测动作
                                #         用于验证推理是否正常
